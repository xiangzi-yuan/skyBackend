<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        select distinct sd.setmeal_id
        from setmeal_dish sd
        <where>
            <if test="dishIds != null and dishIds.size() > 0">
                sd.dish_id in
                <foreach collection="dishIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="dishIds == null or dishIds.size() == 0">
                1 = 0
            </if>
        </where>
    </select>

    <select id="getSetmealNamesByDishIds" resultType="java.lang.String">
        select distinct s.name
        from setmeal_dish sd
        inner join setmeal s on sd.setmeal_id = s.id and s.is_deleted = 0
        <where>
            <if test="dishIds != null and dishIds.size() > 0">
                sd.dish_id in
                <foreach collection="dishIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="dishIds == null or dishIds.size() == 0">
                1 = 0
            </if>
        </where>
    </select>

    <select id="getDishSetmealRelations" resultType="com.sky.readmodel.dish.DishSetmealRelationRM">
        select d.id as dishId, d.name as dishName, s.name as setmealName
        from setmeal_dish sd
        inner join setmeal s on sd.setmeal_id = s.id and s.is_deleted = 0
        inner join dish d on sd.dish_id = d.id
        <where>
            <if test="dishIds != null and dishIds.size() > 0">
                sd.dish_id in
                <foreach collection="dishIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="dishIds == null or dishIds.size() == 0">
                1 = 0
            </if>
        </where>
        order by d.name, s.name
    </select>

    <insert id="insertBatch">
        insert into setmeal_dish(setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishes" item="setmealDish" separator=",">
            (#{setmealDish.setmealId}, #{setmealDish.dishId}, #{setmealDish.name}, #{setmealDish.price}, #{setmealDish.copies})
        </foreach>
    </insert>



    <select id="listDishItemsBySetmealId" resultType="com.sky.readmodel.dish.DishItemRM">
        select
            d.name        as name,
            sd.copies     as copies,
            d.image       as image,
            d.description as description
        from setmeal_dish sd
        inner join dish d on sd.dish_id = d.id
        <where>
            sd.setmeal_id = #{setmealId}
            and d.is_deleted = 0
            and d.status = 1
        </where>
        order by sd.id asc
    </select>

</mapper>
